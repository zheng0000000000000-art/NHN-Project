{
  "name": "team-loop-lite-ai",
  "version": "0.7.0",
  "private": true,
  "description": "Local-first AI loop orchestration, balancing, verification and portable workboard engine.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node --check server.js && node --check public/app.js && node --test --test-concurrency=1",
    "cli": "node bin/team-loop.js",
    "project:pack": "node tools/project-pack.mjs",
    "workspace": "node tools/workspace.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "team-loop": "./bin/team-loop.js",
    "team-loop-mcp": "./mcp/team-loop-mcp.mjs"
  }
}
