# Local-First 판정층 흡수 — 결정 기록

- 상태: **기록만 됨.** 무엇을 합칠지는 미정이며 착수하지 않았다.
- 일시: 2026-07-27
- 결정 주체: 사람(사용자). 조율 세션(Claude Opus 5)이 받아 적음.
- 반대편 기록: `C:\Users\1\Documents\Local-First Workflow Dashboard\docs\handoff\decisions\ADR-018-fusion-into-team-loop.md`

## 왜 이 문서가 지금 생겼나

**결정은 있었는데 어느 저장소에도 없었다.** 2026-07-27에 확인했다 — 이 저장소의 `docs/`,
최근 30커밋, `docs/daily/` 어디에도 융합 언급이 없다(`통합` 매치는 전부 문서 병합 이야기다).
Local-First 쪽 `docs/handoff/`·`docs/plan/`·`decisions/`에도 없었다.

그 결과 **2026-07-27 세션 전체가 Local-First의 C# 개발에 쓰였다.** 인수인계만 보고는
방향을 알 수 없었기 때문이다. 같은 일이 반복되지 않도록 양쪽에 남긴다.

## 결정

**방향은 흡수 — Team Loop이 받는 쪽이다.**
**형태는 "오케스트레이션은 Team Loop, 판정층은 Local-First가 대던 것을 흡수"**이며,
인터페이스만 정의하고 **상태 원본을 한 벌로** 둔다.

**무엇을 합칠지는 아직 정하지 않았다.** 그것은 남은 결재다.

이 방향은 이미 있던 `docs/archive/migrations/LOCAL-FIRST-IMPORTS.md`의 입장과 어긋나지 않는다:

> 옛 C# 구현을 통째로 베끼지 않는다. 재사용할 것은 **운영 규칙·실패 분류·하네스 계약**이다.
> 런타임은 Node.js로 유지한다.

## 넘어올 것 — 판정층 계약 v1

**원본은 하나다. 사본을 만들지 마라** — 그러면 아래 표 3행("표를 실재에 못 박는다")이 말하는
바로 그 병에 걸린다. 옮길 때 원본을 옮기고 반대편에는 포인터만 남긴다.

```
C:\Users\1\Documents\Local-First Workflow Dashboard\docs\plan\JUDGMENT-LAYER-CONTRACT-v1.md
```

그 문서가 담은 것: **데이터 계약 4종**(게이트 매니페스트 · 게이트 보고 · 처분 기록 ·
컨텍스트 팩)과 **규칙 6개**. 아래 표는 목차이고, 근거 수치와 계약 본문은 그 문서에 있다.

### 실측 — 언어에 묶인 부분은 작다 (2026-07-27)

게이트 3종의 서로 다른 명령 **14개 중 11개가 언어 무관**(git · JSON · markdown · 프로세스)이고,
C# 특정은 **3개**(`measure` · `build-verify` · `verify-behavior`)뿐이다.
**`POST-COMMIT`은 22검사 전부가 언어 무관 명령이다.**
구현 규모는 `server/Harness/` **23파일 5,417줄** — **다시 쓸 양이지 옮길 양이 아니다.**

### 규칙 목차

**코드가 아니라 규칙이다.** 각 항목의 근거 문서는 Local-First 저장소
`docs/verification/` 아래에 있다.

| 규칙 | 왜 필요한가(실측 근거) | 이 저장소에 얹히는 지점(가설) |
| --- | --- | --- |
| 면제를 **경로가 아니라 그 커밋의 반입**에 묶는다 | 경로 기준이면 "한 번이라도 자동화가 만진 파일"은 그 뒤로 사람이 영원히 자유롭게 고칠 수 있다. 실측: 위반 2 → 3 | `task/tsk_*` worktree·브랜치 병합 판정 |
| **등재되지 않은 픽스처는 시험이 아니다** — 기대값을 실측한 뒤 매니페스트에 넣는다 | 낡은 값 `20`을 단 픽스처가 어느 게이트에도 안 물려 아무도 실행하지 않았다 | 자체 verification 도구의 케이스 등록 |
| **표를 실재에 못 박는다** — 선언과 실측을 구분한다 | 생산자와 검사자가 같은 표를 읽어 대조가 공회전이었다. 케이스를 늘려도 줄여도 통과했다 | 승격 점수·계약 검사 전반 |
| **exit code로 증명되지 않는 수정이 있다** — 그때는 수치를 본다 | 수정 전후 exit code가 둘 다 1이라 exit code로는 아무것도 안 보였다 | 납품 게이트 판정 |
| **런타임을 컨테이너로 격리해야 "어느 것이 돌았나"가 사실이 된다** | CI가 `10.0`이라 이름 붙은 잡에서 실제로는 8이 돌고 있었다 | Node 버전 매트릭스 |
| **영구히 빨간 게이트는 무시된다** — 고치기 전에 켜지 마라 | 준비 안 된 검사를 켜두면 그 게이트 전체가 무시된다 | 새 검사 도입 절차 |

## 넘어가지 않는 것

`TerritoryCheckCli.cs`, `HarnessJson.cs`, `SelfTestCensus.cs`, .NET 8 `TypeInfoResolver` 수정,
`global.json`, .NET 매트릭스 CI — **런타임이 달라 코드로는 안 건너간다.**

## 다음에 정해야 할 것

1. **합칠 대상의 범위** — 판정층 전부인가, 위 규칙표만인가.
2. **상태 원본을 어디에 둘 것인가.** 두 벌로 두면 Local-First가 2026-07-27 하루에 세 번 고친
   문제(같은 정의가 두 벌이라 한쪽만 낡음)를 저장소 규모로 반복한다.
3. **Local-First 저장소의 처분** — 보존·아카이브·폐기.

## 주의

이 문서는 **기록이지 착수가 아니다.** 이 저장소의 코드·설정은 아무것도 바꾸지 않았다.
