# 2026-07-20 작업 기록

## 오늘 한 일

- 작업보드를 사람의 작업 지시 큐로 정리하고 에이전트 대기·실행 상태를 추가했다.
- MCP 작업 목록을 기본적으로 ID·제목·상태만 반환하도록 축약했다.
- 관리자 리뷰를 허용하고, 완료 전에 실제 결과물이 존재하는지 확인하는 전달 게이트를 추가했다.
- 실패 학습 처리 결과를 리뷰 전에 기록하도록 하고 대체 작업의 학습 루프를 마무리했다.
- 리뷰 카드에서 에이전트의 변경 파일, 검증 명령, 실행 도구를 바로 확인하고 결과 문서로 내려받을 수 있게 했다.
- 외부 AI가 저장소를 clone하지 않고 MCP로 파일을 읽고 결과를 제출하도록 원격 작업 경로를 구현했다.
- 외부 MCP 설치·작업·보안·오류 대응 절차를 한국어 통합 문서로 정리했다.
- 컨텍스트 선택량을 사용량에 기록하고, 만료된 quota의 잘못된 `LIVE` 표시를 수정했다.
- 검증된 스킬·하네스는 활성 상태로 유지하고 작업 생성 시 관련도에 따라 자동 배정하도록 정책을 정리했다.

## 검증 및 운영 상태

- 전체 자동 테스트 144개 통과.
- Team Loop 서버를 `C:\NHN Project\unknown-auction` workspace로 재시작하고 상태를 확인했다.
- 컨텍스트 인덱스는 66개 파일, 441개 청크, 약 23.7만 추정 토큰을 색인한다.
- 외부 MCP 패키지를 GitHub에서 `npx`로 받아 12개 도구가 노출되는 것을 확인했다.
- 현재 리뷰 대기 작업은 `[OPS2] 엔진 모듈 분리 지도`, `[B02] UTF-8 단일 파일 빌드와 배포 산출물`이다.

## 실패 학습과 보완

- Markdown 통합 시 삭제된 문서를 검사기가 다시 읽어 `ENOENT`가 발생하는 문제를 수정하고 회귀 테스트를 추가했다.
- MCP 첫 제출이 검증에 실패한 뒤 기존 서버 worktree 때문에 재제출이 막힐 수 있는 문제를 수정하고 반복 제출 테스트를 추가했다.
- 만료 시각이 지난 quota가 최근 수집됐다는 이유로 최신으로 표시되는 문제를 회귀 테스트로 고정했다.
- 고유한 `FAILURE_DERIVED` 학습이 단순히 출처 때문에 아카이브되지 않도록 클린업 정책을 수정했다.
- 현재 클린업 후보는 `scope-guard`와 겹치는 범위 위반 스킬 3개이며, 유효한 실패 학습은 활성 상태로 유지된다.

## 오늘 결정한 원칙

- 외부 AI 연결은 MCP를 공식 경로로 사용한다.
- 외부 AI는 서버 절대 경로나 공식 저장소를 직접 다루지 않는다.
- MCP는 작업과 결과를 전달하고 서버 worktree가 적용·검증을 담당한다.
- 스킬·하네스는 모두 무조건 적용하지 않고 활성 항목 중 관련된 것만 자동 배정한다.
- 클린업은 삭제가 아니라 이력 보존형 아카이브이며, 중복·시험 실패·대체된 항목에 한정한다.
- 사람은 일반적으로 활성화 버튼을 누르지 않고, 수동 제어는 관리자 고급 관리에서만 사용한다.

## 다음 확인 항목

- 실제 외부 PC의 Claude Code 또는 Codex에서 MCP 로그인부터 결과 제출·검증·리뷰까지 한 번 완주한다.
- 배포 이후 AI 요청이 쌓이면 대시보드의 평균 선택 컨텍스트와 전체 대비 선택 비율을 확인한다.
- 리뷰 대기 중인 OPS2와 B02의 결과·검증 근거를 확인하고 승인 또는 반려한다.
- 아직 시험하지 않은 `public-asset-integrity` 하네스의 필요성과 시험 시점을 결정한다.
